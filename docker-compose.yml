services:
  cv-service:
    build: .
    container_name: catan-cv-service
    restart: unless-stopped
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=${PORT:-8000}
